import 'package:flutter/material.dart';

class UltraRayScreen extends StatelessWidget {
  const UltraRayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        '자외선',
        style: TextStyle(color: Colors.white, fontSize: 20),
      ),
    );
  }
}
